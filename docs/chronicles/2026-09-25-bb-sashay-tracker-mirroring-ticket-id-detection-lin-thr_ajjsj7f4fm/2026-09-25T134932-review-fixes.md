---
type: review-response
timestamp: 2026-09-25T134932
responding-to: review findings 2026-09-25T134709 (3 high, 6 medium)
---

## Review fixes applied: all highs and mediums

**Responding to:** code-review findings (3 high, 6 medium).

- HIGH .git hrefs: app.tsx now derives repo bases via `resolveRepoSlug`
  (single parser; https and ssh both handled). Regression test pins the
  .git round-trip.
- HIGH deps: `better-sqlite3` moved to dependencies; fresh `npm ci` +
  build verified.
- MEDIUM 500-cap: client slices per-repo batches to 500 — overflow now
  degrades to partial dots instead of a rejected call.
- MEDIUM one-shot fetch: the repo-base effect re-runs when the sidebar
  project list changes.
- MEDIUM bare catch: non-SQLite errors now log a warning (bb.log injected
  into readGitHubStatuses); SQLite-native errors stay silent (documented
  degrade paths).
- MEDIUM cleanup: unused `repo` prop dropped from TicketChip; trailing
  newlines added to the four new files; KEY_REF comments corrected
  (lookbehind claim + lookahead job); 500-cap documented;
  GitHubItemStatus documented; $HOME join noted; visibleRefKey
  round-trip commented.
- Bonus low fixes: HASH_REF lookbehind (adjacent `#12#13` both match;
  letters still rejected, new test), per-snapshot status-map replace
  (no stale dots, no unbounded growth), visibleRefKey comment.
- Deferred (recorded): plan-contract note + README bullets land in the
  final documentation commit.

Green: 55/55 tests, tsc clean, plugin build succeeds.

**Commits in this unit:** (this commit)
